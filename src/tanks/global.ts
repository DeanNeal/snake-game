export const WINDOW_SIZE = (window.innerHeight  < window.innerWidth) ? (window.innerHeight - 50) : (window.innerWidth - 50) ;
export const TILE_SIZE = WINDOW_SIZE / 15;
export const BULLET_SPEED = 400;